/// Enum representing the user's preferred first day of the week in the calendar.
enum FirstDayOfWeek {
  /// The system's regional default first day of the week.
  system,

  /// Week starts on Monday.
  monday,

  /// Week starts on Sunday.
  sunday,
}
