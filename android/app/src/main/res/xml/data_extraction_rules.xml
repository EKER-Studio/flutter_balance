<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude local encrypted database files and secure storage keys -->
        <!-- because device-bound hardware Keystore keys cannot be restored from cloud backup. -->
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <!-- Exclude hardware-bound keys and encrypted databases during direct device transfer -->
        <exclude domain="database" />
        <exclude domain="sharedpref" />
    </device-transfer>
</data-extraction-rules>
